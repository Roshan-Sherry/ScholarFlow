# Fine-Tuning Dependencies (install separately)
# Only needed if you plan to fine-tune the model

# Efficient fine-tuning library
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Core ML libraries
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
peft>=0.6.0
trl>=0.7.0
accelerate>=0.24.0

# Data collection
arxiv>=1.4.8

# Utilities
scipy
bitsandbytes
